# 子组件透明接收依赖

> 领域：cxalio-studio-tools（应用框架）· 适用范围：cxalio-studio-tools 框架与全部工具

决定子组件按需接收 `context`/`appenv`/`progress`，而不是接收 `parent: SomeApplication` 引用。

## Consequences

- 职责边界清晰——子组件只接收它需要的。
- 可测试性——可以独立 mock context/appenv。
- 可复用性——不绑定特定 Application 类型。
